mess = input("Enter you messages: ")
secret = input("What you want code or decode: ")
new_word = mess
word = secret
length = len()
if secret == word:
    print(length)
    if length >= 3:
        s = new_word.remove(0)
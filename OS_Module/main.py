import os

# if(not os.path.exists("data")):
#     os.mkdir("data") # to create a new directory

# for i in range(0, 10):
    # os.mkdir(f"data/Tutorial{i+1}") # to create a directory under parent directory
    # os.rename(f"data/Tutorial{i+1}", f"data/Tutorial {i+1}") # to rename a directory
    # os.rmdir((f"data/Tutorial{i+1}")) # removes an empty directory
    # os.remove(f"data/Tutorial 14/tutorial.md") # removes a file